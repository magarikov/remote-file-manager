# remote-file-manager
(they all should be RCP, not RPC)  
RPC_app - server  
RPC_client - client  
but in this case client works as a logical server, because it collects information from connected machines.  
RPC_app works on remote machine and provide some info about system to the connected device  
